<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme2" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/action_bar_bg</item>
        <item name="colorPrimaryDark">@color/status_bar_bg</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorHint">@color/toolbar_hint</item>
    </style>

    <style name="AppThemeMain" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/action_bar_bg</item>
        <item name="colorPrimaryDark">@color/toolbar_hint</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="drawerArrowStyle">@style/toggle.style</item>
        <item name="titleTextColor">@color/colorToolbarTitle</item>
        <item name="searchIcon">@color/colorToolbarTitle</item>
        <item name="android:windowActivityTransitions">true</item>
    </style>

    <style name="AppThemeRecipe" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/action_bar_bg</item>
        <item name="colorPrimaryDark">@color/toolbar_hint</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="drawerArrowStyle">@style/toggle.style</item>
        <item name="titleTextColor">@color/colorToolbarTitle</item>
        <item name="searchIcon">@color/colorToolbarTitle</item>
        <item name="android:windowActivityTransitions">true</item>
    </style>

    <style name="toggle.style" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/colorToolbarTitle</item>
    </style>

    <style name="AppTheme.Toolbar" parent="AppTheme2">
        <!--This line changes the color of text in Toolbar-->
        <item name="android:textSize">16sp</item>
        <item name="android:textColorPrimary">@color/colorToolbarTitle</item>
        <!--This line changes the color of icons in toolbar (back, overflow menu icons)-->
        <item name="android:textColorSecondary">@color/colorToolbarTitle</item>
    </style>

    <style name="CollapsingToolbarLayoutExpandedTextStyle" parent="AppTheme">
        <item name="android:textColor">@android:color/transparent</item>
        <item name="android:textSize">0sp</item>
    </style>

    <style name="SplashScreenTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

</resources>
